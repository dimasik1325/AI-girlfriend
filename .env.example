# Скопируй в .env и заполни — тогда ключ будет жить на сервере,
# а сайт подхватит его автоматически (без ввода в браузере).
PORT=3000
HOST=0.0.0.0

# openai | openrouter | groq
LLM_PROVIDER=openai
LLM_API_KEY=
# Необязательно: модель по умолчанию
LLM_MODEL=gpt-4o-mini
